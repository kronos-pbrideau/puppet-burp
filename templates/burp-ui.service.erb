[Unit]
Description=burp-ui init script
After=network.target

[Service]
User=burp-ui
ExecStart=<%= @burp_ui_binary_path %> -c /etc/burp-ui/burp-ui.conf

Type=idle

[Install]
WantedBy=multi-user.target
